<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- Console appender for development (human-readable) -->
    <springProfile name="dev,local">
        <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
        <!-- More verbose logging for dev -->
        <logger name="com.northernchile" level="DEBUG"/>
    </springProfile>

    <!-- Production: JSON to console + file with rotation -->
    <springProfile name="!dev &amp; !local">
        <!-- Console JSON (for docker logs) -->
        <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <includeMdcKeyName>requestId</includeMdcKeyName>
                <includeMdcKeyName>userId</includeMdcKeyName>
                <includeMdcKeyName>traceId</includeMdcKeyName>
                <customFields>{"application":"northernchile-api"}</customFields>
            </encoder>
        </appender>

        <!-- File appender with rotation (low memory footprint) -->
        <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>/app/logs/app.log</file>
            <!-- Small buffer = less RAM usage -->
            <immediateFlush>false</immediateFlush>
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <!-- Daily rotation + size limit -->
                <fileNamePattern>/app/logs/app.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
                <maxFileSize>20MB</maxFileSize>
                <!-- Keep 14 days of logs -->
                <maxHistory>14</maxHistory>
                <!-- Max 500MB total (comprimido ~100MB real) -->
                <totalSizeCap>500MB</totalSizeCap>
                <!-- Clean old logs on startup -->
                <cleanHistoryOnStart>true</cleanHistoryOnStart>
            </rollingPolicy>
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <includeMdcKeyName>requestId</includeMdcKeyName>
                <includeMdcKeyName>userId</includeMdcKeyName>
                <customFields>{"application":"northernchile-api"}</customFields>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="JSON"/>
            <appender-ref ref="FILE"/>
        </root>
        <logger name="com.northernchile" level="INFO"/>
    </springProfile>

    <!-- Silence noisy loggers -->
    <logger name="org.springframework.web" level="WARN"/>
    <logger name="org.hibernate.SQL" level="WARN"/>
    <logger name="org.hibernate.type.descriptor.sql" level="WARN"/>
</configuration>
