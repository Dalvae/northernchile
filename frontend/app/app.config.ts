export default defineAppConfig({
  // Nuxt UI configuration
  // Los colores semánticos (primary, secondary, etc.) ahora se definen
  // directamente en los archivos CSS de cada tema usando las variables --ui-color-*
  ui: {}
})
